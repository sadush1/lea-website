# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Node (for future tooling — build steps, bundlers, etc.)
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# Env / secrets (for future backend: newsletter API, e-commerce keys, etc.)
.env
.env.local

# Build output (for future framework migration)
dist/
build/
.next/
